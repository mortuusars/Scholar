package io.github.mortuusars.scholar.client.lectern;

import io.github.mortuusars.scholar.client.BookRenderState;

public interface ScholarBookHolderRenderState {
    BookRenderState scholar$getBookRenderState();
    void scholar$setBookRenderState(BookRenderState bookRenderState);
}
